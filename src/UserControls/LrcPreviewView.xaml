<UserControl x:Class="QBLyricEditor.UserControls.LrcPreviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             d:DesignHeight="305"
             d:DesignWidth="600"
             mc:Ignorable="d">
    <Grid>
        <ListView Name="PreviewListPanel"
                  Margin="0"
                  BorderThickness="1"
                  FontSize="16"
                  SelectionMode="Single"
                  FontFamily="{StaticResource LyricFont}"
                  ScrollViewer.CanContentScroll="False"
                  MouseDoubleClick="PreviewListPanel_MouseDoubleClick">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="80" />
                            <ColumnDefinition Width="10" />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <TextBlock HorizontalAlignment="Center" Text="{Binding LrcTimeText}" />
                        <TextBlock Grid.Column="1"
                                   HorizontalAlignment="Center"
                                   Text="|"
                                   Foreground="#ddd" />
                        <TextBlock Grid.Column="2" Text="{Binding LrcText}" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
